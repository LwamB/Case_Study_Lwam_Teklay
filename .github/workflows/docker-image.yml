name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build and push Docker image
    - name: DockerHub Build And Push
      uses: lwamb/my-image-name:tagname/Dockerhub-Build-Push@v1.0
   
      
